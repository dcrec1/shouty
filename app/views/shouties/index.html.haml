- semantic_remote_form_for @shouty do |form|
  = form.inputs :body
  = form.buttons
  
.shouties= render :partial => "shared/shouty", :collection => @shouties
= periodically_call_remote :url => fetch_shouties_path, :frequency => '10', :update => '.shouties', :method => :get